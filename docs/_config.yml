title: ZFC
description: ZFC proofs in Markdown, LaTeX, and Lean 4
lang: en

markdown: kramdown
kramdown:
  input: GFM
  math_engine: mathjax
  syntax_highlighter: rouge

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - README.md
